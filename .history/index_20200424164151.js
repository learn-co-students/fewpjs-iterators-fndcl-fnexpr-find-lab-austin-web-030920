const testVar = {}

function testFunc() {
  return "hi"
}


function superbowlWin(array) {
  
  if (!!(array.find(function (game) { return game.result === "W" }))) {
    // console.log(victory.year)
    return victory.year
  } else {
    return undefined
  }
}